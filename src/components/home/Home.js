import Header from "./header";
import classes from "./Home.module.css";

const Home = () => {
  return (
    <div className={classes.container}>
      <Header />
    </div>
  );
};

export default Home;
